This contains programs related to 
-->counting words which start with letter M or m and letters can change based on requirement, we can use character array or 
    even a db to store the values which are needed to be checked in a list of strings
-->list of strings whose length is more than 5, this can change as well based on user's requirement and can be directly checked on each string 


It includes test cases of both the programs which can be used to have a better understanding of program with sample data